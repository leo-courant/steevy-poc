"""RAG logic: XML parsing, chunking, embedding, Qdrant storage, and ingestion.

This package owns all retrieval-augmented-generation *code*. The XML files
themselves live in the `data/rag/` drop-zone, never here.
"""
